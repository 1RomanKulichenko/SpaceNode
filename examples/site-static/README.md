# Static site example

This example serves the repository's `/public` folder as a static website using SpaceNode's static file server.

Run from the repository root:

```bash
node examples/site-static/index.js
```

Open: http://localhost:3003
