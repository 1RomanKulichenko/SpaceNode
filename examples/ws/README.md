# WebSocket example

This example shows a minimal WebSocket endpoint using `app.ws()`.

Run:

```bash
node examples/ws/index.js
```

Connect with `wscat` or browser WebSocket client to `ws://localhost:3002/ws`.
